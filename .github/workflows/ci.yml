name: CI

on: [push, pull_request]

jobs:
  unit-test:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout
      - uses: actions/setup-go
      - name: Run Unit Tests
        run: make unit-tests